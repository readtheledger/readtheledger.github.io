# captured publisher feeds - regenerate with ./fetch_fixtures.sh, don't commit
fixtures/

# test artefacts
shot-*.png
__pycache__/
.DS_Store

# build output (build.mjs) and the previous release the upgrade test installs first
_site/
_old/
_site_unstamped/

# the gathered Newsstand (fetch_feeds.mjs writes it; the workflow gathers a fresh one)
data/

# Cloudflare delivery consumes the already-published Pages artifact.
_cloudflare_site/
.github/cloudflare/node_modules/
.wrangler/
